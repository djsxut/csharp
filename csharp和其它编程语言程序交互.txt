1. C#如果需要调用其它编程语言实现的程序：
  可以把程序打包为dll库，提供接口, 方便C#调用:
    对于Java程序, 可以使用ikvmc转换jar包为dll, 或直接通过ikvmbin调用代码;
    对于C/C++/delphi/VB, 可以直接打包出dll
  
2. 如果要通过C#操作不同语言程序的界面：
  如果程序是console，那么可以直接调用；
  如果程序是GUI界面，如果没有组件支撑，可行的方法是记录按键位置进行操作；